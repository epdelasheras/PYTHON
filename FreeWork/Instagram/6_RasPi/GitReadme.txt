Add new files to the repo
git add my_new_file.tx
To do a commit of the staged files:
git commit -a -m "Change titles and styling on homepage"
To push previous commit:
git push origin master
To update the repository:
git pull
